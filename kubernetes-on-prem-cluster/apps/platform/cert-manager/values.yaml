installCRDs: true

# Configure all components to use cert-manager namespace for leader election
# instead of kube-system (which requires additional RBAC permissions)
global:
  leaderElection:
    namespace: cert-manager
